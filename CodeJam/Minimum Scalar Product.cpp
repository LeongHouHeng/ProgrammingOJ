#include <iostream>
#include <vector>
#include <fstream>
using namespace std;

int main(){
    
    int t;
    cin >> t;
    for(int cnt = 1; cnt <= t; cnt++){
        int n;
        vector<int> va, vb;
        cin >> n;
        for(int j = 0; j < n; j++){
            int tmp;
            cin >> tmp;
            va.push_back(tmp);
        }
        for(int j = 0; j < n; j++){
            int tmp;
            cin >> tmp;
            vb.push_back(tmp);
        }
        cout << "Case #" << cnt << ": ";
        int minNum = 1000000;
        for()
        
    }
    
    return 0;
}