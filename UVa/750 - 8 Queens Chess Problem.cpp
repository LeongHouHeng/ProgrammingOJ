#include <iostream>
#include <vector>
#include "memory.h"

using namespace std;
vector<int> ans;
map[10][10];
int round;
void dfs(int x, int y, int roundCount){
    if(map[x][y])
    
    
}

int main(){
    
    int test;
    cin >> test;
    while(test--){
        memset(map, 0, sizeof(map));
        ans.clear();
        int row, column;
        cin >> row >> column;
        cout << "SOLN       COLUMN" << endl;
        cout << "#      1 2 3 4 5 6 7 8" << endl;
        dfs(row, column, 1)
    }
    
    return 0;
}