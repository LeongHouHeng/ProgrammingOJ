#include <iostream>
#include <string>
#include <algorithm>
#include <vector>
using namespace std;

struct countrys{
    string country;
    int gold;
    int silver;
    int bronze;
    int total;
}

int main(){
    vector<countrys> vc;
    countrys cs;
    vector<string>
    int n;
    string input;
    cin >> n;
    while(n--){
        getline(cin, input);
        split(fields, input, is_any_of(","));
        
    }
    
    return 0;
}